# Evidence 4.2

On this page, we provide Julia code to verify that the formula for $\Omega_{dH}$ on $\text{Tot}(\mathcal{O}_{\mathbb{P}^2}(-1)^{\oplus 3})$ given by [main_paper; equation (29)](@cite) matches the values computed from Gromov-Witten theory.

The GKM graph is obtained using [`gkm_5d_P2_111`](@ref).
The curve classes `b, 2*b, 3*b, 4*b` in the code below correspond to $H, 2H, 3H, 4H$ in [main_paper; Table 3](@cite).
Under each line calling [`get_Omega_beta`](@ref), we recorded the crucial line of the output as a comment.

```@eval
using Markdown
Markdown.parse("```julia\n" * read(joinpath(@__DIR__, "..", "..", "..", "test", "results", "P2_111.jl"), String) * "\n```")
```