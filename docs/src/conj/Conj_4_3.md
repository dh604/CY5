# Section 4.3

This page concerns an example $Z=X\times\mathbb{C}^2$ where $X$ does not come from a 3d strip geometry,
and $Z$ falsifies Conjecture 4.3 since $\Omega_{2\beta}\neq 0$ for a certain $\beta$.

!!! note
    TODO: discuss if and how we want to present this.

```@eval
using Markdown
Markdown.parse("```julia\n" * read(joinpath(@__DIR__, "..", "..", "..", "test", "results", "free_P1_chain.jl"), String) * "\n```")
```
