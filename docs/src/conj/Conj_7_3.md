# Section 7.3

!!! note
    This page concerns $\mathcal{O}_{\mathbb{P}^3}(-2)^{\oplus 2}$ in Section 7.3.
    TODO: clarify structure.

## Verified cases

### Testing the conjectural formula

!!! note
    TODO: expand test range. In particular, for degree 4 there should be nonzero stuff in low genus, while in degree 3 there should be nonzero stuff in higher genus.

```@eval
using Markdown
Markdown.parse("```julia\n" * read(joinpath(@__DIR__, "..", "..", "..", "test", "results", "P3_22.jl"), String) * "\n```")
```

### Independence of the antidiagonal scaling parameter

```@eval
using Markdown
Markdown.parse("```julia\n" * read(joinpath(@__DIR__, "..", "..", "..", "test", "results", "P3_m2_m2.jl"), String) * "\n```")
```
